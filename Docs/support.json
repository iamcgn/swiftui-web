{
  "sections": [
    {
      "title": "App lifecycle",
      "entries": [
        {
          "api": "App / Scene / WindowGroup / @main",
          "status": "partial",
          "notes": "App/Scene/WindowGroup/SceneBuilder API; main() mounts the first WindowGroup in the canvas host (wasm) or lays out headlessly; Tier B (Chromium) within tolerance"
        }
      ]
    },
    {
      "title": "View composition",
      "entries": [
        {
          "api": "View / ViewBuilder (if/else, optional, switch, #available, any child count)",
          "status": "partial",
          "notes": "Runtime node tree with structural identity; no layout or painting yet; Tier B (Chromium) within tolerance"
        },
        {
          "api": "EmptyView / TupleView / Group / _ConditionalContent / Optional",
          "status": "partial",
          "notes": "Runtime nodes exist; no layout yet; Tier B (Chromium) within tolerance"
        },
        {
          "api": "AnyView",
          "status": "partial",
          "notes": "Runtime nodes exist; no layout yet; Tier B (Chromium) within tolerance"
        },
        {
          "api": "ViewModifier / ModifiedContent / EmptyModifier / modifier(_:)",
          "status": "partial",
          "notes": "Runtime nodes exist; no layout yet; Tier B (Chromium) within tolerance"
        },
        {
          "api": "EnvironmentValues / EnvironmentKey / environment(_:_:) / transformEnvironment",
          "status": "partial",
          "notes": "@Entry macro not provided; Tier B (Chromium) within tolerance"
        },
        {
          "api": "Transaction / TransactionKey / withTransaction",
          "status": "stub",
          "notes": "No `animation` member until the animation system exists"
        },
        {
          "api": "id(_:) / IDView",
          "status": "partial",
          "notes": "Identity change rebuilds the subtree; Tier B (Chromium) within tolerance"
        },
        {
          "api": "ForEach (Identifiable, id:, Range<Int>, Binding collections) / DynamicViewContent",
          "status": "partial",
          "notes": "Keyed reconciliation: state follows ids across insert/mutate/reorder/remove (foreach/identity, 4 steps); modifiers distribute per element; no editActions, onDelete/onMove, or Subviews-based forms; Tier B (Chromium) exact frames, ≤ 0.3 % pixels incl. every identity step",
          "fixtures": [
            "foreach/*"
          ]
        },
        {
          "api": "Section (content/header/footer, title forms, deprecated argument orders)",
          "status": "partial",
          "notes": "Transparent outside List/Form: header, content and footer flatten in order, exact against goldens; inside List: styled header/footer, spacing, pinned first header (list/sections); no Form styling, isExpanded or collapsible; Tier B (Chromium) exact frames, ≤ 0.3 % pixels",
          "fixtures": [
            "section/*"
          ]
        },
        {
          "api": "DynamicProperty (custom, nested)",
          "status": "partial",
          "notes": "update() and nested installation; Tier B (Chromium) within tolerance"
        },
        {
          "api": "#Preview / PreviewTrait / PreviewProvider",
          "status": "stub",
          "notes": "The macro expands to nothing (SwiftUIWebMacros, prebuilt swift-syntax) so preview blocks compile; nothing renders previews",
          "fixtures": []
        }
      ]
    },
    {
      "title": "Views",
      "entries": [
        {
          "api": "Text (verbatim/localized/interpolation, concatenation with mixed styles)",
          "status": "partial",
          "notes": "Layout exact with recorded metrics on 17 fixtures; default font is the 13 pt system font and bold() resolves per text style (decision 0010); concatenated parts keep font/weight/traits/colour; wrapping, character wrapping, hard newlines, lineLimit (Int, ranges, reservesSpace), truncationMode head/middle/tail, multilineTextAlignment, lineSpacing; no localization tables, kerning/tracking, attributed strings, height-pressure truncation; Tier B (Chromium) exact frames on all 17 text fixtures, pixels ≤ 1.9 % (system-font fallbacks 4.7 %); WebKit exact frames, text pixels ≤ 1.1 %; Firefox exact frames, pixels ≤ 3.5 % (hinting)",
          "fixtures": [
            "text/*"
          ]
        },
        {
          "api": "lineLimit / multilineTextAlignment / truncationMode / lineSpacing (View + EnvironmentValues)",
          "status": "partial",
          "notes": "All lineLimit overloads (lower bounds reserve lines); truncation at character granularity with spaces dropped next to the ellipsis; alignment shifts lines by drawn width; a text under height pressure does not drop lines yet; Tier B (Chromium) exact frames on all 17 text fixtures, pixels ≤ 1.9 % (system-font fallbacks 4.7 %); WebKit exact frames, text pixels ≤ 1.1 %; Firefox exact frames, pixels ≤ 3.5 % (hinting)",
          "fixtures": [
            "text/line-limit",
            "text/truncation",
            "text/alignment",
            "text/line-spacing"
          ]
        },
        {
          "api": "allowsTightening / minimumScaleFactor",
          "status": "stub",
          "notes": "Environment values stored, not applied to layout"
        },
        {
          "api": "VStack / HStack / ZStack",
          "status": "partial",
          "notes": "Layout exact against goldens; no painting yet; Tier B (Chromium) within tolerance",
          "fixtures": [
            "layout/*"
          ]
        },
        {
          "api": "Spacer",
          "status": "partial",
          "notes": "Layout exact; no painting yet; Tier B (Chromium) within tolerance",
          "fixtures": [
            "layout/spacer",
            "layout/spacer-min-length"
          ]
        },
        {
          "api": "Button",
          "status": "partial",
          "notes": "bordered (default), borderedProminent, plain exact in layout; borderless approximate (grey label in a window); custom ButtonStyle; press state; no roles/disabled; Tier B (Chromium) within tolerance",
          "fixtures": [
            "button/basic",
            "button/styles"
          ]
        },
        {
          "api": "Toggle (isOn:label:, title, image, systemImage, configuration) / ToggleStyle / ToggleStyleConfiguration / toggleStyle",
          "status": "partial",
          "notes": "Checkbox (default), switch and button styles measured in layout and on/off pixels; activation on release inside; checkbox semantics (aria-checked); no pressed/hover/focus looks, keyboard, mixed state or Toggle(sources:)",
          "fixtures": [
            "toggle/basic",
            "toggle/styles",
            "toggle/steps"
          ]
        },
        {
          "api": "Label (title:icon:, image, systemImage) / LabelStyle (automatic, titleAndIcon, titleOnly, iconOnly) / labelStyle",
          "status": "partial",
          "notes": "Icon centred on half the cap height above the title's baseline, 8 pt spacing (measured); systemImage is the stub symbol; the automatic style is not context sensitive",
          "fixtures": [
            "label/basic"
          ]
        },
        {
          "api": "List (content, selection, data/id/Range forms) / ListStyle (automatic, inset, plain, bordered, sidebar) / listStyle",
          "status": "partial",
          "notes": "macOS row geometry, separators, section headers/footers with the pinned first header, styles, single and multiple selection by press, labels' icon slot and tint measured; no outline lists, edit actions, keyboard selection, focused (accent) highlight, alternating backgrounds or custom styles",
          "fixtures": [
            "list/basic",
            "list/sections",
            "list/styles",
            "list/modifiers",
            "list/steps"
          ]
        },
        {
          "api": "NavigationStack (root, NavigationPath and collection path bindings) / NavigationPath / NavigationLink (destination, value forms)",
          "status": "partial",
          "notes": "Content-sized stack (macOS hosted window), pushed views centred with earlier views laid out beneath, bordered-button links outside lists and plain row links inside, Runtime.navigateBack() for hosts; no back button/title chrome, animation, toolbars, navigationDestination(item:)",
          "fixtures": [
            "nav/basic",
            "nav/list",
            "nav/title",
            "nav/sizing",
            "nav/steps"
          ]
        },
        {
          "api": "NavigationSplitView (two/three columns, columnVisibility, preferredCompactColumn) / NavigationSplitViewVisibility / navigationSplitViewColumnWidth / NavigationSplitViewStyle",
          "status": "partial",
          "notes": "The macOS 26 split: a white sidebar panel inset 8 pt (140 wide by default, corner radius 7.5), a 200 pt content column with a 1 pt divider, the detail centred in the rest; column widths (ideal clamped to min/max), visibility through the binding, Runtime.toggleSidebar() and ⌃⌘S; sidebar lists take the sidebar style; links in the leading columns push onto the detail; no toolbar button, material, drag-resizing or collapse animation",
          "fixtures": [
            "splitview/basic",
            "splitview/widths",
            "splitview/three",
            "splitview/columns",
            "splitview/sized",
            "splitview/selection",
            "splitview/visibility"
          ]
        },
        {
          "api": "Picker (title, string, custom label) / PickerStyle (automatic, menu, segmented, radioGroup, inline, palette) / pickerStyle / tag",
          "status": "partial",
          "notes": "macOS pop-up button, segmented control and radio group geometry measured; segmented and radio selection by press; the pop-up opens a menu through the presentation layer (approximate look); text options only in the pop-up and segmented styles",
          "fixtures": [
            "picker/basic",
            "picker/forms",
            "picker/steps"
          ]
        },
        {
          "api": "Slider (value, range, step, label, min/max value labels, onEditingChanged)",
          "status": "partial",
          "notes": "16 pt flexible track with a 22 × 16 knob, ticks when stepped, body label and footnote value labels pixel-aligned; presses jump and drags follow; no keyboard control or vertical sliders",
          "fixtures": [
            "slider/basic",
            "slider/steps"
          ]
        },
        {
          "api": "Stepper (value/step/bounds and closure forms, onEditingChanged)",
          "status": "partial",
          "notes": "20 × 26 chevron button pair 8 pt after the body label; top half increments, bottom half decrements within bounds; no press-and-hold repeat or keyboard control",
          "fixtures": [
            "stepper/basic",
            "stepper/steps"
          ]
        },
        {
          "api": "Form / FormStyle (automatic, columns, grouped) / formStyle",
          "status": "partial",
          "notes": "Columns layout with a label column and measured control spacing (exact), grouped cards with padded rows and separators, form-aware TextField/Toggle/Picker/Slider/Stepper rows; no LabeledContent, DisclosureGroup, GroupBox; grouped section headers unverified",
          "fixtures": [
            "form/basic",
            "form/sections",
            "form/styles",
            "form/steps"
          ]
        },
        {
          "api": "Layout protocol (custom layouts, caches, LayoutSubviews/LayoutSubview, LayoutValueKey) / AnyLayout / HStackLayout / VStackLayout / ZStackLayout",
          "status": "partial",
          "notes": "Custom layouts written on the public API match Apple's frames exactly; AnyLayout switches layouts keeping subview state; no Animatable layout parameters or right-to-left direction",
          "fixtures": [
            "customlayout/flow",
            "customlayout/radial",
            "customlayout/values",
            "customlayout/any"
          ]
        },
        {
          "api": "Grid / GridRow / gridCellColumns / gridColumnAlignment / gridCellAnchor / gridCellUnsizedAxes",
          "status": "partial",
          "notes": "Column sizing, spans, flexible sharing, spacing from cell preferences, alignment and anchors match Apple's frames exactly; no GridLayout value or lazy grids",
          "fixtures": [
            "grid/basic",
            "grid/spacing",
            "grid/modifiers",
            "grid/alignment",
            "grid/flexible"
          ]
        },
        {
          "api": "Canvas / GraphicsContext (fill, stroke, text, transforms, opacity, clip, drawLayer) / ResolvedText",
          "status": "partial",
          "notes": "Immediate-mode drawing recorded into the display list (a concat op for rotated text); pixels within 0.11 % of Apple's; gradient shadings, images, blend modes, filters or symbols",
          "fixtures": [
            "canvas/basic",
            "canvas/sizing"
          ]
        },
        {
          "api": "Gradient / LinearGradient / RadialGradient / AngularGradient / HierarchicalShapeStyle (fills, strokes, backgrounds, text, Canvas shading)",
          "status": "partial",
          "notes": "Display-list gradient ops painted by Canvas2D, stops blended in Oklab (≤ 0.08 % pixels); text foreground gradients through an offscreen mask; Canvas gradient shadings; .primary keeps the inherited style; no elliptical or mesh gradients, image gradients",
          "fixtures": [
            "gradient/basic",
            "gradient/text"
          ]
        },
        {
          "api": "Menu / MenuStyle / menuIndicator / contextMenu",
          "status": "partial",
          "notes": "Pull-down and split buttons exact; menus, submenus and context menus through the presentation layer (rows, separators; no hover highlight, checked items, section headers or keyboard navigation)",
          "fixtures": [
            "menu/basic"
          ]
        },
        {
          "api": "onKeyPress / onMoveCommand / onExitCommand / onDeleteCommand / keyboardShortcut / focusable / KeyEquivalent / EventModifiers / KeyPress",
          "status": "partial",
          "notes": "Key presses dispatched from the focused view outwards, then menus, shortcuts and Escape; general focus with a ring for buttons and focusable views; list arrow-key selection and menu keyboard navigation (looks unverified); no key-up phases, onKeyPress(characters:), focus sections or default focus",
          "fixtures": [
            "keyboard/basic"
          ]
        },
        {
          "api": "ProgressView (value/total, labels, currentValueLabel) / ProgressViewStyle (automatic, linear, circular) / ControlSize / controlSize(_:) / tint(_:)",
          "status": "partial",
          "notes": "Determinate bars and rings measured exactly (inactive-window greys); the indeterminate spinner and bar are the animation's first frame; tint accepted without effect; no Progress objects or timer intervals",
          "fixtures": [
            "progress/basic",
            "progress/indeterminate"
          ]
        },
        {
          "api": "Gauge (value/bounds, label, current/minimum/maximum value labels, marked labels accepted) / GaugeStyle (automatic, linearCapacity, accessoryLinear, accessoryLinearCapacity, accessoryCircular, accessoryCircularCapacity) / gaugeStyle",
          "status": "partial",
          "notes": "The macOS linear capacity gauge (13 pt label over a 16 pt tinted bar, value and bounds labels at the stack spacings) and the monochrome accessory styles: the 8 pt track with a knob and 17 pt semibold labels, the capsule capacity bar with a 12 pt secondary value, the 58 pt open ring (270°, trimmed 14° per end for bounds labels) with a marker and 24 pt medium value, the capacity ring; no animation or marked value labels",
          "fixtures": [
            "gauge/basic",
            "gauge/accessory"
          ]
        },
        {
          "api": "DatePicker (selection, displayedComponents, in: ranges, title/label forms) / DatePickerComponents / DatePickerStyle (automatic, compact, field, stepperField, graphical) / datePickerStyle / EnvironmentValues.timeZone, calendar",
          "status": "partial",
          "notes": "The macOS textual field: tabular two-digit slots in a text-field bezel with a 12.5 × 20 mini stepper (the field style without it, 21 tall); component selection by press, stepping by the stepper and arrow keys, ranges clamp; the graphical calendar (13 pt bold header, 10 pt bold weekdays, 11 pt days in 18.5 × 18 cells, paging and day selection) and the display-only clock; en_US Gregorian in the environment's time zone; no typing, popover or hand dragging",
          "fixtures": [
            "datepicker/basic",
            "datepicker/styles",
            "datepicker/graphical",
            "datepicker/clock",
            "datepicker/steps"
          ]
        },
        {
          "api": "TextEditor(text:) / TextEditorStyle (automatic, plain) / textEditorStyle / scrollContentBackground",
          "status": "partial",
          "notes": "The macOS text view: fills its proposal, white background (removable), black text 5 pt in with the text view's 12 pt pitch for 13 pt and the line spacing, font and colour modifiers; editing through the host's multi-line input (a textarea in the browser, typing natively); NSTextView's tighter letter spacing is not reproduced, no find/replace or selection binding",
          "fixtures": [
            "texteditor/basic",
            "texteditor/sizing",
            "texteditor/styles",
            "texteditor/steps"
          ]
        },
        {
          "api": "Table(_:columns:) / selection (single, set) / sortOrder / TableColumn (key path, custom content) / width(_:) / width(min:ideal:max:) / KeyPathComparator",
          "status": "partial",
          "notes": "The macOS table: 28 pt header with dividers, sorted title bold with a chevron, 24 pt rows with alternating rounded bands and the selection band, cells 8 pt into their columns; NSTableView's column distribution measured (117 pt automatic pitch, grow/shrink/overflow regimes, fixed and flexible widths); selection by press and arrow keys (Shift extends), sorting by header presses; no row scrolling, column resizing, TableRow builders, table styles or hidden headers",
          "fixtures": [
            "table/basic",
            "table/widths",
            "table/sized",
            "table/selection",
            "table/sorting",
            "table/regimes"
          ]
        },
        {
          "api": "ColorPicker(_:selection:supportsOpacity:) / ColorPicker(selection:label:)",
          "status": "partial",
          "notes": "The macOS colour well: 48 × 24 grey rounded rect with the colour in a concentric swatch over a black/white diagonal (translucent and clear colours show it), inner stroke, half opacity disabled; body label centred in stacks, on the well's baseline in forms; a press opens a preset popover (16 swatches, opacity slider) standing in for the system colour panel; no native colour inputs, keyboard activation or CGColor bindings",
          "fixtures": [
            "colorpicker/basic",
            "colorpicker/labels",
            "colorpicker/form",
            "colorpicker/sized",
            "colorpicker/steps"
          ]
        },
        {
          "api": "GroupBox (content, title, custom label) / GroupBoxStyle (automatic) / groupBoxStyle(_:)",
          "status": "partial",
          "notes": "macOS 26 card (radius 12, black 8/255, content padded 5) under a subheadline label; measured exactly; no dark appearance or Form insets",
          "fixtures": [
            "groupbox/basic"
          ]
        },
        {
          "api": "LabeledContent (content:label:, title, value:) / LabeledContentStyle (automatic) / labeledContentStyle(_:)",
          "status": "partial",
          "notes": "Label (body font) + 8 + content centred, the form's label column inside a Form; no FormatStyle values",
          "fixtures": [
            "labeledcontent/basic",
            "labeledcontent/form"
          ]
        },
        {
          "api": "Link / OpenURLAction / EnvironmentValues.openURL",
          "status": "partial",
          "notes": "Link blue (0, 104, 218), half opacity disabled, label snapped to the pixel grid; openURL with custom handlers, the browser opens a tab and the native host asks NSWorkspace; no ShareLink or hover looks",
          "fixtures": [
            "link/basic"
          ]
        },
        {
          "api": "DisclosureGroup (title, isExpanded:, custom label) / DisclosureGroupStyle (automatic) / disclosureGroupStyle(_:)",
          "status": "partial",
          "notes": "A full-width 24 pt row (grey chevron, 5, label, 4 above/below) toggling the content centred below; own state without a binding; no animation, outline rows in lists or OutlineGroup",
          "fixtures": [
            "disclosure/basic"
          ]
        },
        {
          "api": "LazyVStack / LazyHStack / LazyVGrid / LazyHGrid / GridItem (fixed, flexible, adaptive) / PinnedScrollableViews",
          "status": "partial",
          "notes": "Eager: stacks fill their cross axis; grids resolve fixed/flexible (clamped, no redistribution)/adaptive tracks and flow cells line by line, centred in the frame; no laziness, pinned headers or sections in grids",
          "fixtures": [
            "lazy/stacks",
            "lazy/grids"
          ]
        },
        {
          "api": "TabView (selection, own state) / tabItem / TabViewStyle (automatic)",
          "status": "partial",
          "notes": "The macOS tab view: a segmented bar of the tab item titles (title + 24, dividers) over a bordered box, the selected content centred beneath; press and arrow keys select; no images/badges in the bar, no iOS Tab API or page style",
          "fixtures": [
            "tabview/basic",
            "tabview/sized"
          ]
        },
        {
          "api": "ContentUnavailableView (label/description/actions, title+image, .search)",
          "status": "partial",
          "notes": "Large bold secondary title (title only, as the macOS 26 golden shows), body description, actions; top-aligned in the space it fills",
          "fixtures": [
            "unavailable/basic"
          ]
        },
        {
          "api": "ShareLink (item(s), title, custom label) / ShareAction.systemHandler",
          "status": "partial",
          "notes": "A bordered button with the share symbol; the browser's Web Share and the native NSSharingServicePicker; no Transferable items or previews",
          "fixtures": [
            "sharelink/basic"
          ]
        },
        {
          "api": "TimelineView / TimelineSchedule (periodic, everyMinute, explicit, animation, custom)",
          "status": "partial",
          "notes": "Wakes through main-actor Task.sleep (no Foundation timers on wasm); the animation schedule follows host frames; no lowFrequency distinction or Timer APIs",
          "fixtures": [
            "timeline/basic"
          ]
        },
        {
          "api": "TextField (title/prompt/label forms) / SecureField / TextFieldStyle (automatic, roundedBorder, squareBorder, plain) / textFieldStyle",
          "status": "partial",
          "notes": "Bezel, insets, baseline, placeholder, bullets and disabled look measured; editing through the host's transparent <input> (browser caret/selection/IME); no @FocusState, multi-line (axis), formatted values or custom styles",
          "fixtures": [
            "textfield/basic",
            "textfield/styles",
            "textfield/steps"
          ]
        },
        {
          "api": "Divider",
          "status": "partial",
          "notes": "Layout exact (1pt); no painting yet; Tier B (Chromium) within tolerance",
          "fixtures": [
            "layout/divider"
          ]
        },
        {
          "api": "Color (as a view)",
          "status": "partial",
          "notes": "macOS light system colour table; painting via display list; Tier B (Chromium) within tolerance",
          "fixtures": [
            "paint/system-colors"
          ]
        },
        {
          "api": "Layout protocol / custom layouts / layoutValue",
          "status": "partial",
          "notes": "sizeThatFits, placeSubviews, spacing, explicitAlignment, cache; no RTL; Tier B (Chromium) within tolerance"
        },
        {
          "api": "Font (text styles, system(size:weight:design:), bold/weight/italic/monospaced)",
          "status": "partial",
          "notes": "macOS text-style table; bold() is a per-style trait; italic keys; Tier B (Chromium) within tolerance",
          "fixtures": [
            "text/styles",
            "text/system-fonts",
            "text/modifiers"
          ]
        },
        {
          "api": "Rectangle / RoundedRectangle (circular, continuous) / UnevenRoundedRectangle / Circle / Ellipse / Capsule",
          "status": "full",
          "notes": "Paths identical to Apple's element for element (74 recorded paths, PathGoldenTests): continuous corner curves, radius limits, start points; Tier B pixels ≤ 0.24 % Chromium, ≤ 0.11 % WebKit",
          "fixtures": [
            "shape/builtin",
            "shape/layout",
            "paint/shapes"
          ]
        },
        {
          "api": "Path (move/line/curves, addArc/addRelativeArc, rects, ellipses, rounded rects, description and init?(_:), applying, contains, trimmedPath, forEach)",
          "status": "full",
          "notes": "Element order, arc sweep rules and trimming match Apple; addArc(tangent1End:…) implemented from CGPath's definition, unverified; no cgPath",
          "fixtures": [
            "shape/path",
            "shape/modifiers"
          ]
        },
        {
          "api": "Path.strokedPath(_:)",
          "status": "approximate",
          "notes": "Oriented polygons per segment, join and cap (a nonzero fill unions them), dashes honoured; Apple computes the offset outline. Painters stroke natively, so this only affects clipping and hit testing with a stroked shape",
          "fixtures": [
            "shape/modifiers"
          ]
        },
        {
          "api": "Shape.fill / stroke, StrokeStyle (caps, joins, miter limit, dash, phase), FillStyle (eoFill), FillShapeView / StrokeShapeView / StrokeBorderShapeView chaining",
          "status": "full",
          "notes": "antialiased flags stored without effect (canvas always antialiases); Tier B within tolerance in three browsers",
          "fixtures": [
            "shape/stroke",
            "shape/path",
            "shape/steps"
          ]
        },
        {
          "api": "InsettableShape / inset(by:) / strokeBorder",
          "status": "full",
          "notes": "Rounded rectangles shrink their radii by the inset like Apple; an inset shape lays out like a plain shape (measured)",
          "fixtures": [
            "shape/stroke",
            "shape/modifiers"
          ]
        },
        {
          "api": "Shape modifiers: trim / offset / scale / rotation / transform / size / stroke(style:) as a Shape, AnyShape",
          "status": "full",
          "notes": "Only trim and stroke keep the base shape's layout (a trimmed circle stays square); the others take the proposal (measured)",
          "fixtures": [
            "shape/modifiers",
            "shape/layout"
          ]
        },
        {
          "api": "ContainerRelativeShape",
          "status": "approximate",
          "notes": "A rectangle; container shapes are not modelled",
          "fixtures": []
        },
        {
          "api": "Shape boolean operations (union / intersection / subtracting / symmetricDifference / lineIntersection / lineSubtraction)",
          "status": "missing",
          "notes": "",
          "fixtures": []
        },
        {
          "api": "Angle",
          "status": "full",
          "notes": "radians/degrees, Comparable, Animatable",
          "fixtures": [
            "shape/path"
          ]
        },
        {
          "api": "GeometryReader / GeometryProxy (size, frame(in:), bounds(of:))",
          "status": "partial",
          "notes": "No safe area or anchors; Tier B (Chromium) within tolerance",
          "fixtures": [
            "all (probe implementation)"
          ]
        },
        {
          "api": "ScrollView (axes, showsIndicators) / ScrollViewReader / ScrollViewProxy.scrollTo",
          "status": "partial",
          "notes": "Fills the proposal along its axes and is exactly content-sized across them, implicit VStack content, clipping, defaultScrollAnchor, scrollTo with/without anchor resolved at layout; wheel scrolling with chaining, touch pan + momentum (0.998/ms), overlay indicators while scrolling (approximate geometry); no bounce, keyboard scrolling, scrollPosition/scrollTargetBehavior, contentMargins or lazy stacks; Tier B exact frames in Chromium, WebKit and Firefox (17/17 renders each)",
          "fixtures": [
            "scroll/*"
          ]
        },
        {
          "api": "Image(_:bundle:) / Image(decorative:bundle:) / Image(_:bundle:label:) from asset catalogs",
          "status": "partial",
          "notes": "scripts/assets.py reads *.xcassets into a manifest (decision 0011): image sets with 1x/2x/3x, mac/universal idiom, light/dark appearance, folder namespaces, template intent; PNG/JPEG/GIF. Rigid at pixels ÷ scale; missing name is 0 × 0. Tier A exact; Tier B exact frames in Chromium, WebKit and Firefox. Open: PDF/SVG sets, slicing metadata, dark-appearance goldens, bundle: ignored (absent on wasm)",
          "fixtures": [
            "image/intrinsic",
            "image/stack-spacing",
            "image/swap"
          ]
        },
        {
          "api": "Image.resizable(capInsets:resizingMode:) / renderingMode / interpolation / antialiased",
          "status": "partial",
          "notes": "Stretch, nine-part stretch, tile and nine-part tile; template tint from the foreground style; interpolation(.none) is nearest neighbour, other levels smooth; antialiased stored only. Cap insets with .tile draw all nine parts where AppKit drops the bottom row (1.6 % pixels in image/tiling)",
          "fixtures": [
            "image/resizable",
            "image/template",
            "image/tiling"
          ]
        },
        {
          "api": "Image(systemName:) / Image.Scale / imageScale(_:) / View.fontWeight / View.bold",
          "status": "partial",
          "notes": "Measured SF Symbol layout (240 symbols at the text-style sizes, 13 pt weights and image scales; scaled elsewhere) with Lucide icons (ISC) standing in for the glyphs, 1551 names; no rendering modes, variants or effects",
          "fixtures": [
            "symbol/basic",
            "symbol/catalog-13"
          ]
        },
        {
          "api": "Image(nsImage:) / Image(cgImage...) / Image(size:label:renderer:) / AsyncImage",
          "status": "missing",
          "notes": "No CGImage/NSImage on wasm"
        },
        {
          "api": "Color(_:bundle:) from asset catalog colour sets / ColorScheme / colorScheme environment",
          "status": "partial",
          "notes": "sRGB components in float, hex and integer spellings; the light or dark variant per environment.colorScheme (default light; nothing else reads it yet). Display P3 components are used as sRGB. A missing name is clear (assumed)",
          "fixtures": [
            "color/named"
          ]
        },
        {
          "api": "AsyncImage / Image(nsImage:) / Image(cgImage:)",
          "status": "missing",
          "notes": "Only asset-catalog images and SF Symbol stand-ins are drawn"
        },
        {
          "api": "Toolbar / ToolbarItem / toolbar(_:) / navigationBar customisation",
          "status": "missing",
          "notes": "Not implemented; NavigationStack shows its title only"
        },
        {
          "api": "searchable(_:) / Searchable modifiers",
          "status": "missing",
          "notes": "Not implemented"
        },
        {
          "api": "Chart (Swift Charts) / Map / VideoPlayer / WebView",
          "status": "missing",
          "notes": "Separate frameworks, not part of the SwiftUI module yet"
        },
        {
          "api": "Sheet and window management: WindowGroup identifiers, openWindow, Settings scene, MenuBarExtra",
          "status": "missing",
          "notes": "Only the first WindowGroup's root is shown"
        }
      ]
    },
    {
      "title": "State",
      "entries": [
        {
          "api": "@State",
          "status": "partial",
          "notes": "Box per node; writes coalesce; no animation transactions yet; Tier B (Chromium) within tolerance"
        },
        {
          "api": "@Binding",
          "status": "partial",
          "notes": "get/set, constant, dynamic member lookup, optional and collection projections; Tier B (Chromium) within tolerance"
        },
        {
          "api": "@Environment",
          "status": "partial",
          "notes": "Key-path and Observable-object forms (`Environment(Model.self)`, optional variant); `.environment(object)`; Tier B (Chromium) within tolerance"
        },
        {
          "api": "@Observable / @Bindable",
          "status": "partial",
          "notes": "Per-body withObservationTracking; only reading nodes invalidate. Bindable via ReferenceWritableKeyPath; Tier B (Chromium) within tolerance"
        },
        {
          "api": "ObservableObject / @Published / @StateObject / @ObservedObject / @EnvironmentObject",
          "status": "partial",
          "notes": "Combine-free implementation (ObservableObjectPublisher, AnyCancellable, @Published through the enclosing-instance subscript); typealiases in the SwiftUI shim shadow Foundation's Combine re-export on macOS; no onReceive or Combine operators (Docs/elements/ObservableObject.md)",
          "fixtures": [
            "observable/object"
          ]
        },
        {
          "api": "ObservableObject / ObservableObjectPublisher / AnyCancellable / @Published / @StateObject / @ObservedObject / @EnvironmentObject / environmentObject",
          "status": "partial",
          "notes": "Combine-free implementation: closure subscribers, enclosing-instance @Published, per-identity state objects, re-subscription on replacement; no Combine operators or onReceive",
          "fixtures": [
            "observable/object"
          ]
        },
        {
          "api": "@FocusState (Bool, optional Hashable) / focused(_:) / focused(_:equals:)",
          "status": "partial",
          "notes": "Text fields only; state and browser focus mirror each other in both directions (click, Tab, blur, programmatic); no focusable(), FocusedValue, key handling; focus on buttons and focusable views too (Docs/elements/Keyboard.md)",
          "fixtures": [
            "focus/basic"
          ]
        }
      ]
    },
    {
      "title": "Modifiers",
      "entries": [
        {
          "api": "padding",
          "status": "partial",
          "notes": "Layout exact; Tier B (Chromium) within tolerance",
          "fixtures": [
            "layout/padding-default",
            "layout/padding-edges"
          ]
        },
        {
          "api": "frame",
          "status": "partial",
          "notes": "Fixed and flexible forms exact; Tier B (Chromium) within tolerance",
          "fixtures": [
            "layout/frame-fixed",
            "layout/frame-flex"
          ]
        },
        {
          "api": "background / overlay",
          "status": "partial",
          "notes": "View, style and shape forms; layout exact, display-list painting; Tier B (Chromium) within tolerance; one layer per element when applied to a list (foreach/modifier, section/modifier)",
          "fixtures": [
            "paint/background-overlay"
          ]
        },
        {
          "api": "foregroundStyle / foregroundColor",
          "status": "partial",
          "notes": "Flat colours only; Tier B (Chromium) within tolerance"
        },
        {
          "api": "fixedSize / layoutPriority / alignmentGuide",
          "status": "partial",
          "notes": "Layout exact; Tier B (Chromium) within tolerance",
          "fixtures": [
            "layout/fixed-size",
            "layout/hstack-priority",
            "layout/alignment-guide"
          ]
        },
        {
          "api": "font(_:)",
          "status": "partial",
          "notes": "Environment font; Tier B (Chromium) within tolerance",
          "fixtures": [
            "text/modifiers"
          ]
        },
        {
          "api": "opacity / clipShape / clipped / cornerRadius",
          "status": "partial",
          "notes": "Display-list groups and clips; Tier B (Chromium) within tolerance; applied per element on lists",
          "fixtures": [
            "paint/clipping"
          ]
        },
        {
          "api": "preference / transformPreference / onPreferenceChange / coordinateSpace",
          "status": "partial",
          "notes": "Bottom-up reduction after layout; no anchorPreference or overlayPreferenceValue yet; Tier B (Chromium) within tolerance"
        },
        {
          "api": "onTapGesture",
          "status": "partial",
          "notes": "Single tap via the pointer arena; count ignored; Tier B (Chromium) within tolerance"
        },
        {
          "api": "scrollIndicators / scrollDisabled / scrollBounceBehavior / scrollClipDisabled / defaultScrollAnchor",
          "status": "partial",
          "notes": "Environment-backed; scrollBounceBehavior is stored but has no effect (no rubber band); defaultScrollAnchor sets the initial offset only",
          "fixtures": [
            "scroll/modifiers",
            "scroll/anchor-bottom"
          ]
        },
        {
          "api": "onChange(of:initial:_:) (two- and zero-argument actions), deprecated onChange(of:perform:)",
          "status": "partial",
          "notes": "Actions run from the scheduler's action queue after the update that changed the value; sibling onChange order is inner first",
          "fixtures": [
            "scroll/scroll-to"
          ]
        },
        {
          "api": "aspectRatio(_:contentMode:) / aspectRatio(_ size:contentMode:) / scaledToFit / scaledToFill / ContentMode",
          "status": "full",
          "notes": "Ratio explicit or from the content's ideal size; the content is proposed the fitted or filling rectangle and keeps the last word (rigid images stay rigid). Exact against goldens for full, partial and absent proposals",
          "fixtures": [
            "image/aspect-ratio"
          ]
        },
        {
          "api": "border(_:width:)",
          "status": "full",
          "notes": "An inset rectangle stroke overlaid on the content; no layout change (measured)",
          "fixtures": [
            "shape/border"
          ]
        },
        {
          "api": "labelsHidden()",
          "status": "full",
          "notes": "Controls drop their label and its spacing (measured on Toggle)",
          "fixtures": [
            "toggle/basic",
            "toggle/styles"
          ]
        },
        {
          "api": "disabled(_:) / EnvironmentValues.isEnabled",
          "status": "partial",
          "notes": "Blocks activation of buttons and toggles; toggles dim their control and label (measured), buttons do not dim yet",
          "fixtures": [
            "toggle/basic"
          ]
        },
        {
          "api": "onSubmit(_:) / onSubmit(of:_:) / SubmitTriggers",
          "status": "partial",
          "notes": "Return in a text field runs the action; triggers ignored",
          "fixtures": []
        },
        {
          "api": "autocorrectionDisabled(_:)",
          "status": "stub",
          "notes": "Stored only",
          "fixtures": []
        },
        {
          "api": "listRowInsets / listRowBackground / listRowSeparator / listRowSeparatorTint",
          "status": "partial",
          "notes": "Per-row insets, edge-to-edge background, bottom separator visibility and tint (top edge ignored); apply per element through a ForEach or Section",
          "fixtures": [
            "list/modifiers"
          ]
        },
        {
          "api": "listSectionSeparator / listSectionSeparatorTint / listItemTint",
          "status": "stub",
          "notes": "Stored only",
          "fixtures": []
        },
        {
          "api": "navigationDestination(for:) / navigationDestination(isPresented:) / navigationTitle",
          "status": "partial",
          "notes": "Destinations registered by type with the enclosing stack, isPresented pushes and pops through observation, the title is recorded on the runtime (window chrome on macOS)",
          "fixtures": [
            "nav/basic",
            "nav/steps"
          ]
        },
        {
          "api": "navigationSubtitle / navigationBarBackButtonHidden",
          "status": "stub",
          "notes": "Stored only",
          "fixtures": []
        },
        {
          "api": "onAppear / onDisappear / task(priority:) / task(id:priority:)",
          "status": "partial",
          "notes": "Actions run through the scheduler after the update that inserts or removes the view; tasks start on appear, cancel on disappear and restart on an id change; no onReceive or scene-phase actions",
          "fixtures": [
            "lifecycle/appear"
          ]
        },
        {
          "api": "Animation (linear, ease curves, timingCurve, springs, delay/speed/repeat) / withAnimation / Transaction.animation",
          "status": "partial",
          "notes": "Curves evaluated per frame; frames, opacity and Color fills tween; no transforms, shape/text colour, Animatable custom data, completion callbacks or blending",
          "fixtures": [
            "animation/frame",
            "animation/implicit"
          ]
        },
        {
          "api": "animation(_:value:) / transaction(_:) / transition(_:) / AnyTransition (identity, opacity, move, slide, offset, combined, asymmetric)",
          "status": "partial",
          "notes": "Scopes take precedence over the transaction for their subtree; removed views linger as ghosts through their transition; .scale fades (no transforms yet)",
          "fixtures": [
            "animation/implicit",
            "animation/transition"
          ]
        },
        {
          "api": "sheet / popover (isPresented and item forms) / alert / confirmationDialog / dismiss environment action",
          "status": "approximate",
          "notes": "Presented over the window by the runtime's presentation layer (modal sheets and alerts, popovers and menus dismissed outside); the looks approximate macOS (separate windows there, no goldens); no fullScreenCover, detents, Menu, contextMenu",
          "fixtures": [
            "presentation/basic"
          ]
        },
        {
          "api": "accessibilityLabel / Hint / Value / Identifier / Hidden / AddTraits / RemoveTraits / accessibilityElement(children:)",
          "status": "partial",
          "notes": "Applied on the semantics tree that the canvas host mirrors as an ARIA DOM overlay (headings, images, groups, switches, range inputs, spinbuttons); no custom actions, sort priority or rotors",
          "fixtures": [
            "accessibility/basic"
          ]
        },
        {
          "api": "offset / rotationEffect / scaleEffect / transformEffect / AnyTransition.scale",
          "status": "partial",
          "notes": "Painted through the display list's concat op about their anchors, parameters animate, offsets move hit testing; no hit testing through rotation/scale, no GeometryEffect or 3D",
          "fixtures": [
            "transform/basic",
            "transform/steps"
          ]
        },
        {
          "api": "shadow(color:radius:x:y:) / Color.RGBColorSpace inits",
          "status": "full",
          "notes": "A shadow group in the display list (op 18); the browser and CoreGraphics painters blur the composite with sigma = radius; the default third-opaque linear black; Tier C pixel-identical (Docs/elements/Effects.md)",
          "fixtures": [
            "effects/shadow",
            "effects/shadow-profile",
            "effects/shadow-offset"
          ]
        },
        {
          "api": "blur / brightness / contrast / saturation / hueRotation / grayscale / blendMode / colorInvert / colorMultiply / luminanceToAlpha",
          "status": "missing",
          "notes": "Colour and blend effects are not painted yet (Phase 6)"
        },
        {
          "api": "zIndex / hidden",
          "status": "full",
          "notes": "zIndex reorders painting and hit testing among a container's children (ties keep declaration order); hidden keeps the layout and removes the view from painting, hit testing and semantics",
          "fixtures": [
            "effects/zindex",
            "effects/hidden"
          ]
        },
        {
          "api": "position / mask / compositingGroup / drawingGroup",
          "status": "missing",
          "notes": "Not implemented (Phase 6)"
        },
        {
          "api": "ignoresSafeArea / safeAreaInset / safeAreaPadding",
          "status": "missing",
          "notes": "Not implemented; the browser window has no safe areas"
        },
        {
          "api": "onHover / help / pointerStyle / onContinuousHover",
          "status": "missing",
          "notes": "Not implemented (Phase 6)"
        },
        {
          "api": "gesture / DragGesture / LongPressGesture / MagnifyGesture / RotateGesture / simultaneousGesture / highPriorityGesture",
          "status": "missing",
          "notes": "Only onTapGesture; drags exist for sliders and scrolling only (Phase 6)"
        },
        {
          "api": "underline / strikethrough (Text and View, Text.LineStyle patterns and colours) / textCase / baselineOffset",
          "status": "partial",
          "notes": "Lines at CoreText's underline offset and half x-height, snapped to device pixels, weight and design ratios; patterns in multiples of the thickness; textCase before measurement; baselineOffset grows the text and moves the baseline guide as Apple does. Some weights draw the line one pixel off (Docs/elements/TextStyle.md)",
          "fixtures": [
            "textstyle/underline",
            "textstyle/strikethrough",
            "textstyle/patterns",
            "textstyle/case",
            "textstyle/baseline"
          ]
        },
        {
          "api": "kerning / tracking / textSelection / textScale",
          "status": "missing",
          "notes": "Not implemented (Phase 6)"
        },
        {
          "api": "matchedGeometryEffect / contentTransition / keyframeAnimator / phaseAnimator / PhaseAnimator / KeyframeAnimator",
          "status": "missing",
          "notes": "Not implemented"
        },
        {
          "api": "dropDestination / draggable / onDrag / onDrop / pasteboard",
          "status": "missing",
          "notes": "Not implemented"
        },
        {
          "api": "redacted / privacySensitive / unredacted",
          "status": "missing",
          "notes": "Not implemented"
        },
        {
          "api": "symbolEffect / symbolRenderingMode / symbolVariant",
          "status": "missing",
          "notes": "SF Symbols are Lucide stand-ins; symbol effects are not implemented"
        }
      ]
    }
  ]
}
